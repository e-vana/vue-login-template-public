{
  "scripts": {
    "start": "cd server && node index.js",
    "heroku-postbuild": "cd server && npm install && cd .. && cd client && npm install && npm i -g vue-template-compiler && npm run build"
  }
}